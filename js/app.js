function setView(v){const rab=document.getElementById('rabFullview');if(v==='3d'){open3DModal();return;}document.querySelectorAll('.tab-btn').forEach(b=>b.classList.remove('active'));if(v==='rab'){document.getElementById('vtab-rab').classList.add('active');rab.classList.add('show');recalcRAB();}else{document.getElementById('vtab-2d').classList.add('active');rab.classList.remove('show');}}function switchPanel(name){if(name==='mep'&&typeof requireFeature==='function'&&!requireFeature('mep'))return;document.querySelectorAll('.panel-view').forEach(v=>v.classList.remove('active'));document.querySelectorAll('.ptab').forEach(t=>t.classList.remove('active'));document.getElementById('pv-'+name).classList.add('active');document.querySelector(`.ptab[data-panel="${name}"]`)?.classList.add('active');if(name==='mep'&&typeof renderMepPanel==='function')renderMepPanel();}function applyCompass(){document.getElementById('compassDial').style.transform=`rotate(${northAngle}deg)`;document.getElementById('compassDeg').textContent=`${Math.round(((northAngle%360)+360)%360)}°`;}(function initCompass(){const dial=document.getElementById('compassDial');let dragging=false;function angleFromEvent(e){const r=dial.getBoundingClientRect();const cx=r.left+r.width/2,cy=r.top+r.height/2;return Math.atan2(e.clientY-cy,e.clientX-cx)*180/Math.PI+90;}dial.addEventListener('mousedown',e=>{dragging=true;e.preventDefault();});window.addEventListener('mousemove',e=>{if(!dragging)return;northAngle=Math.round(angleFromEvent(e));applyCompass();render();});window.addEventListener('mouseup',()=>{if(dragging){dragging=false;saveSnapshot();}});dial.addEventListener('dblclick',()=>{northAngle=0;applyCompass();render();});})();document.addEventListener('keydown',e=>{if(e.target.tagName==='INPUT'||e.target.tagName==='SELECT')return;const map={v:'select',s:'swall',r:'room',w:'wall',d:'door',j:'window'};if(map[e.key.toLowerCase()])setTool(map[e.key.toLowerCase()]);if(e.key.toLowerCase()==='f'){switchPanel('furn');showNotif('🪑 Panel Furnitur');}if(e.key.startsWith('Arrow')){e.preventDefault();const step=e.shiftKey?120:45;if(e.key==='ArrowLeft')panOffset.x+=step;if(e.key==='ArrowRight')panOffset.x-=step;if(e.key==='ArrowUp')panOffset.y+=step;if(e.key==='ArrowDown')panOffset.y-=step;render();return;}if(e.key==='Escape'){close3DModal();if(typeof cancelLandDraw==='function')cancelLandDraw();if(typeof closeDev3D==='function')closeDev3D();if(typeof closeAIRender==='function')closeAIRender();if(typeof closeCheckout==='function')closeCheckout();if(typeof closePlanModal==='function')closePlanModal();closeDrawings&&closeDrawings();closeAIStudio&&closeAIStudio();if(typeof closePriceEditor==='function')closePriceEditor();document.getElementById('rabFullview').classList.remove('show');if(typeof disarmMep==='function')disarmMep();if(typeof swCancel==='function')swCancel();setTool('select');selectRoom(null);deselectFurn();selectedMepId=null;selectedSegId=null;render();}if((e.ctrlKey||e.metaKey)&&e.key==='z'&&!e.shiftKey){e.preventDefault();undo();}if((e.ctrlKey||e.metaKey)&&(e.key==='y'||(e.key==='z'&&e.shiftKey))){e.preventDefault();redo();}if((e.ctrlKey||e.metaKey)&&e.key==='s'){e.preventDefault();saveProject();}if(e.key==='Delete'||e.key==='Backspace'){if(selectedOpening)deleteOpening(selectedOpening.kind,selectedOpening.id);else if(selectedSegId)deleteSeg(selectedSegId);else if(selectedMepId)deleteMep(selectedMepId);else if(selectedFurnId)deleteFurn();else if(selectedRoom){deleteRoom(selectedRoom);showNotif('🗑 Ruangan dihapus');}}if(e.key==='PageUp'){e.preventDefault();switchFloor(currentFloorIndex+1);}if(e.key==='PageDown'){e.preventDefault();switchFloor(currentFloorIndex-1);}});function init(){resizeCanvas();initFurnPanel();if(typeof initPlan==='function')initPlan();if(typeof initCloud==='function')initCloud();if(typeof applySavedPrices==='function')applySavedPrices();if(typeof setMepCat==='function')setMepCat('elec');if(typeof renderWallPanel==='function')renderWallPanel();if(typeof detectRooms==='function')detectRooms();syncSiteInputs();applyCompass();renderFloorTabs();renderRoofGrid();updateRoomList();updateStats();recalcRAB();renderSelectedRoomProps();setTool('select');if(!localStorage.getItem(SAVE_KEY)){addRoom('Ruang Tamu',120,160,200,160);addRoom('Kamar Tidur',340,160,160,160);addRoom('Dapur',120,340,160,120);addRoom('Kamar Mandi',300,340,80,80);selectRoom(null);undoStack=[];showNotif('👋 Selamat datang di Floor Planner 2.0');}else{setTimeout(()=>showNotif('💾 Ada proyek tersimpan — klik Muat untuk membuka'),500);}resetZoom();}window.addEventListener('load',init);